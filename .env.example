# === GitLab Credentials ===

# Base URL of your GitLab instance (e.g. https://gitlab.com or self-hosted)
GITLAB_URL=https://gitlab.com

# GitLab personal access token with API access (not base64, plain token)
GITLAB_TOKEN=glpat-your-gitlab-token-here

# === PMD Configuration ===

# Absolute path to the PMD executable
PMD_PATH=/path/to/pmd-bin-7.7.0/bin/pmd

# Path to your custom ruleset XML for PMD
PMD_RULESET=/path/to/ruleset.xml

# Path to suppressions XML file for excluding specific violations
PMD_SUPPRESSIONS=/path/to/suppressions.xml